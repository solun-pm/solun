DATABASE_URL=postgresql://user:password@localhost:5432/solun
PORT=3001
FRONTEND_URL=http://localhost:3000
# REQUIRED: the API refuses to start while this is left at the placeholder.
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_SECRET=change-me-to-a-random-32-char-secret-key
